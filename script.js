// Animate motion for plane (Education section)
document.addEventListener("DOMContentLoaded", () => {
    const plane = document.querySelector("animateMotion");
    if (plane) {
      plane.setAttribute("dur", "10s");
      plane.setAttribute("repeatCount", "indefinite");
    }
  });
  
